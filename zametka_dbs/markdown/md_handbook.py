HANDBOOK_MD = """# Руководство Zametka

## Обзор

Zametka — быстрый редактор Markdown-заметок с привязкой к папке (vault).
Ядро приложения написано на Rust для максимальной производительности,
интерфейс — на Python (PyQt6).

---

## Возможности

- **Редактор Markdown** с подсветкой синтаксиса для 20+ языков
- **Предпросмотр** MD → HTML в реальном времени (движок Comrak, Rust)
- **Прикрепление файлов/папок** (Pinned) — быстрый доступ к важному
- **Полнотекстовый поиск** по заметкам (TF-IDF, Rust)
- **Вики-ссылки** `[[note]]` и обратные связи (backlinks)
- **Дерево файлов** с навигацией как в VS Code
- **Сборка в единый `.exe`** — работает из коробки

---

## Интерфейс

### Левая панель (сайдбар)

1. **ШАПКА VAULT**
   - Кнопка меню (Open File, Create File, Open Folder, Close Folder, Save, Save As...)
   - Кнопка «Daily Note» — создать/открыть сегодняшнюю заметку
   - Кнопка «Handbook» — это руководство

2. **Pinned (прикреплённое)**
   - Закрепляйте файлы и папки для быстрого доступа
   - Кнопка `+` → «Pin file...» или «Pin folder...»
   - Для файлов отображается языковой бейдж (например, `Python`)
   - Для папок сканируются расширения файлов (до 5 языков)
   - Правый клик → Unpin, Remove missing paths
   - Список автоматически прокручивается при большом количестве элементов

3. **Дерево файлов**
   - Отображает все файлы в открытой папке (vault)
   - Двойной клик по файлу — открыть в редакторе
   - Двойной клик по папке — развернуть/свернуть (не теряется корень проекта)
   - Правый клик → Open with Zametka / Notepad / системной программой
   - Правый клик на файле → Move to folder, Open file location

4. **Backlinks (обратные связи)**
   - Показывает, какие заметки ссылаются на текущую

5. **Поиск** (Ctrl+F)
   - Полнотекстовый поиск по всем `.md` файлам в vault

### Центральная область

- **Вкладки** — открытые файлы
- **Панель инструментов** — Save, Save As…, Toggle Preview (Ctrl+P)
- **Редактор** — подсветка синтаксиса, нумерация строк
- **Предпросмотр** — рендеринг Markdown в HTML (можно скрыть Ctrl+P)

### Строка состояния

- Статус сохранения (Saved / Unsaved)
- Кнопка поиска
- Позиция курсора (Ln X, Col Y)
- Счётчик слов
- Текущий шрифт
- Информационные сообщения

---

## Горячие клавиши

| Комбинация | Действие |
|---|---|
| `Ctrl+N` | Новая заметка |
| `Ctrl+O` | Открыть папку (vault) |
| `Ctrl+S` | Сохранить |
| `Ctrl+P` | Показать/скрыть предпросмотр |
| `Ctrl+F` | Поиск |
| `F11` | Полный экран |

---

## Архитектура (Rust + Python)

### Rust-ядро (`zametka-core/`)

| Модуль | Назначение |
|---|---|
| `config.rs` | Загрузка/сохранение JSON-конфига (`%APPDATA%/Zametka/config.json`) |
| `search.rs` | Полнотекстовый поиск: инвертированный индекс, TF-IDF, рекурсивный обход папок |
| `markdown.rs` | Рендеринг MD→HTML (comrak), парсинг `[[wikilinks]]`, построение backlinks |
| `language.rs` | Определение языка по расширению, сканирование папок |

### Python (интерфейс)

- Все модули сначала пытаются импортировать Rust-версию
- При отсутствии Rust-модуля — прозрачное падение на Python-реализацию

### Сборка

```bash
python build.py
```

Процесс:
1. `maturin develop --release` — компиляция Rust-ядра в `.pyd`
2. `PyInstaller Zametka.spec` — сборка единого `.exe`

Итоговый файл: `dist/Zametka.exe`

---

## Markdown-синтаксис

### Заголовки

```
# h1
## h2
### h3
#### h4
##### h5
###### h6
```

### Форматирование текста

```
**жирный**
*курсив*
~~перечёркнутый~~
`код`
```

### Списки

```
- элемент 1
- элемент 2
  - вложенный

1. первый
2. второй
   1. вложенный
```

### Таблицы

```
| Колонка 1 | Колонка 2 |
|-----------|-----------|
| A         | B         |
```

### Вики-ссылки

```
[[Название заметки]]
[[папка/заметка]]
[[заметка|Текст ссылки]]
```

### Чек-листы

```
- [ ] не выполнено
- [x] выполнено
```

### Блок кода

```python
print("Hello, World!")
```

### Цитаты

```
> Текст цитаты
```

---

## Конфигурация

Файл: `%APPDATA%/Zametka/config.json`

Основные ключи:
- `vault_path` — путь к открытой папке
- `editor.font_size` — размер шрифта в редакторе
- `editor.font_family` — шрифт редактора
- `ui.sidebar_width` — ширина боковой панели
- `pinned.items` — список закреплённых файлов/папок

---

## Устранение неполадок

### Приложение не запускается

Проверьте:
1. Наличие `dist/Zametka.exe` после сборки
2. Отсутствие ошибок в `build/` логах

### Не работает поиск

Убедитесь, что открыта папка (vault). Поиск индексирует только файлы `.md`.

### Не отображаются бейджи языков

Для папок сканирование ограничено глубиной 2 уровня.
Для одиночных файлов бейдж определяется по расширению.

---

## Лицензия

Zametka — проект с открытым исходным кодом.
"""


def get_handbook() -> str:
    return HANDBOOK_MD
